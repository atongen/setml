(jbuild_version 1)

(executable
  ((name test)
   (public_name test)
   (modules (
     card_tests
     clients_tests
     combinatorics_tests
     crypto_tests
     messages_tests
     pubsub_tests
     server_util_tests
     test_sync
     test_async
     test_util
     test))
   (libraries (
     lib
     shared
     oUnit))
   (package setml)))

(alias
 ((name runtest)
  (deps (
    test.exe))
  (action (run ${<}))))
